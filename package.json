{
  "name": "donkey-web",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-first donkeycar: drive, train, autopilot -- zero install.",
  "type": "module",
  "scripts": {
    "serve": "node test/serve.js",
    "test": "bash scripts/test.sh"
  },
  "devDependencies": {
    "puppeteer-core": "^25.4.0"
  }
}
